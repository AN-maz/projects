<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>MessageApp | <%= title %></title>
    <style>
        /* CSS Tambahan agar kolom tidak terlalu rapat di layar kecil */
        .main-content {
            padding-left: 1rem;
            padding-right: 1rem;
        }
    </style>
</head>

<body class="bg-light">
    <div class="container-fluid">
        <div class="row">

            <% if (user) { %>
                <aside class="col-lg-3 col-md-4 border-end p-3 bg-white min-vh-100">
                    <%- include('../partials/_chatPanel') %>
                </aside>
                
                <main class="col-lg-6 col-md-8 mt-4 main-content">
                    <%# Flash Messages %>
                    <%- include('../partials/_flashMessages') %>
                    
                    <%- body %>
                </main>
                
                <aside class="col-lg-3 d-none d-lg-block bg-dark text-white p-3 min-vh-100">
                    <%- include('../partials/_rightSidebar') %>
                </aside>

            <% } else { %>
                <main class="col-md-12 mt-4">
                    <%- include('../partials/_flashMessages') %>
                    <%- body %>
                </main>
            <% } %>

        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>